# TwinxPointClouldPipeline
A local pipeline for generating 3D point clouds from video input using **COLMAP / GLOMAP**.   Designed to be extended into a **web-based system** (API + worker architecture).
